# 树状数组
## 📁 快速计算lowbit(i)代码模版
```text
int lowbit(int x){
        return x & -x;
}
## 📁 单值修改代码模版
```text
void update(int x,int y){//a[x]增加y
        while(x<=n){
            c[x]+=y;
            x+=lowbit(x);    
        }
    }
